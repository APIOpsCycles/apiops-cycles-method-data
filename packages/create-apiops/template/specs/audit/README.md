Store audit outputs here, for example:

- design-audit.md
- delivery-audit.md
- publishing-audit.md

By default, the audit tooling uses the canonical package checklist:

- `apiops-cycles-method-data/snippets/api-audit-checklist.json`

Only add `api-audit-checklist.json` here if this project needs a local override of the default checklist.
