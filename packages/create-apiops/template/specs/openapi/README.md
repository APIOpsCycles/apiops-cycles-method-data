The scaffolded `specs/openapi/api.yaml` file is created during project setup.

It is copied from the canonical package example:

- `apiops-cycles-method-data/snippets/api-contract-example.yaml`

After scaffolding, `specs/openapi/api.yaml` becomes the local project-owned API contract file.
