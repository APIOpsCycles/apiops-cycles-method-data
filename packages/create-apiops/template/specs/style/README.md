Optional local style-guide overrides go here.

By default, this project uses the canonical package asset:

- `apiops-cycles-method-data/snippets/api-style-guide.json`

Only add `api-style-guide.json` here if this project needs to customize or replace the default style guide locally.
